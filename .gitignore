# Dependencies
node_modules/

# Build outputs
dist/
out/

# Packaged extension
*.vsix

# macOS
.DS_Store

# Source maps
*.js.map

# Bundled assets — generated by npm run build, not committed
# These are copies of relay-server/dist and mobile-ui synced into the extension
extension/server/
extension/mobile-ui/

# Runtime generated files (persisted token, QR code, etc.)
# These live in ~/.kiro-remote/ on the user's machine, not in the repo
